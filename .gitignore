# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# PyInstaller: el exe se distribuye como asset de Release, no en el repo
build/
dist/
*.spec

# graphify: salida generada. Se puede reconstruir en cualquier momento y no le
# sirve a quien viene al repo a arreglar sus mods, asi que no se versiona.
graphify-out/

# Estado local del inyector (se genera dentro de la carpeta del juego, no aqui,
# pero por si alguien lo copia)
_InjectorState/
_VanillaBackup/
*.bak
*.log
